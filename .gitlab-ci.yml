stages:
  - build

before_script:
  - apt-get update -qq
  - apt-get install -q -y build-essential python-cffi
  - apt-get install -q -y python-all python3-all
  - apt-get install -q -y python-setuptools python3-setuptools python-pip
  - apt-get install -q -y rpm tar gzip bzip2
  - pip install -U pip
  - pip install -r requirements.txt
  - make clean

build-jessie:
  when: always
  stage: build
  image: debian:jessie
  script:
    - make
    - make install
  artifacts:
    paths:
      - dist/ocspd*.tar.gz
      - dist/ocspd*.rpm
      - dist/ocspd*.whl
      - dist/ocspd*.tar.bz2
      - dist/ocspd*.deb

build-stretch:
  when: always
  stage: build
  image: debian:stretch
  script:
    - make
    - make install
  artifacts:
    paths:
      - dist/ocspd*.tar.gz
      - dist/ocspd*.rpm
      - dist/ocspd*.whl
      - dist/ocspd*.tar.bz2
      - dist/ocspd*.deb
