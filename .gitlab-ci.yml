stages:
  - build

before_script:
  - apt-get update -qq
  - apt-get install -q -y build-essential
  - apt-get install -q -y python python-all python3 python3-all
  - apt-get install -q -y python-setuptools python3-setuptools
  - make clean

build-jessie:
  stage: build
  image: debian:jessie
  script:
    - make
  artifacts:
    paths:
      - dist/ocspd*.tar.gz
      - dist/ocspd*.rpm
      - dist/ocspd*.whl
      - dist/ocspd*.tar.bz2
      - dist/ocspd*.deb


build-stretch:
  stage: build
  image: debian:stretch
  script:
    - make
  artifacts:
    paths:
      - dist/ocspd*.tar.gz
      - dist/ocspd*.rpm
      - dist/ocspd*.whl
      - dist/ocspd*.tar.bz2
      - dist/ocspd*.deb
